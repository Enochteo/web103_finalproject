{
  "name": "web103_finalproject",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "cd server && npm install && npm run build-client"
  }
}
